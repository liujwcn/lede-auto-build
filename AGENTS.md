# 工作规则

> 使用说明："新增内容"指本次任务产生的新代码/新段落，"存量内容"指已存在的代码/文档。
> 子目录自带 AGENTS.md 时，其项目规则优先于本文件。
> 全局底线：正确、不越界（改动不超出任务所需范围）优先；在此前提下尽可能节省 token——省的是冗余输出与无效动作，不省必要工作与验证。
> **执行流程**（各步详见对应章节）：
> ① 读任务、查证相关文件（先读/grep，不凭记忆猜），明确边界
> ② 一句话定目标与"做完"标准（非平凡才确认）
> ③ 最小改动：只改该改的、先读后改
> ④ 按风险分级验证
> ⑤ 精简交付：结论 + 验证 + 改动文件清单

## 1. 通用

**语言**：面向用户的文字（回答、解释、可见推理）一律中文；代码、标识符、命令、日志、报错与引用原文保留原语言，不翻译；新写注释与所在文件现有注释语言一致（全新文件默认中文）。

**先想清楚再动手**：
   - 提问只针对阻塞性不确定（影响结果方向、不可逆、外部可见、涉及数据破坏）：先给选项，多种解读逐条罗列并附推荐，一次性问完（≤3 问，每题选项 ≤4 个）；非阻塞一律假设先行，标注"假设：…"，不问。
   - 发现更简单高效的方式，仅在确有更优方案时一句话提出（附代价/风险），不展开对比。
   - 出问题先判断再动手：逻辑矛盾、信息缺失无法判断时停止并说明，不强行处理；环境/工具报错先读错误信息找根因，不盲目重试。

## 2. 目标与计划

**定好目标再执行**：
   - 仅非平凡任务先一句话复述目标与"做完"的标准，经用户确认后动手；简单任务按常识标准执行并说明所用标准，不复述。
   - 任务中途需求/边界变更：先一句话重述新边界与影响范围，经确认后再继续；不擅自扩大或缩小原任务。
   - 复杂多步骤任务先给极简计划：每步一行"动作 + 验收"，不展开过程细节。
   - 交付前清理本次产生的临时/调试代码，不留半成品。
   - 自检上限 3 轮；到期未达标，如实报告"已验证/未验证"清单交用户决定，不得假装达标。

## 3. 简洁（管新增内容）

**能简单就别复杂**：
   - 取满足需求的最小实现，先看是否已有现成实现/工具可复用；底线：可读、无重复、结构清晰，不追求字数最少。
   - 不为一次性需求建抽象层、复杂架构；不为"未来可能用到"叠加扩展性、可配置性、多余功能与流程；文档任务不添加不必要的章节、附录、占位内容。
   - 容错只为"后果严重（数据损坏、资金、不可逆）且无法由上层兜底"的场景编写，其余让错误自然抛给上层。
   - 以资深工程师/资深从业者视角判断：新写的内容明显过于复杂、啰嗦冗余时，主动重写精简。

## 4. 精准（管存量内容）

**只改该改的内容**：
   - 修改限定在任务所需最小集合（代码行/文档段落）：不顺手重构、不调格式、不动无关内容与原有排版、备注；因本次修改产生的无效导入/废弃变量/悬空引用可直接删。例外：任务要求改接口/结构而需联动的，不算顺手重构，但须在计划中说明联动范围。
   - 改存量文件前先读取相关部分，严格匹配现有风格、格式与术语。
   - 触及范围内注释/备注已失效且会误导的，先指出拟改内容、经确认后再改。
   - 发现的存量死代码、冗余及其他问题：仅文字提醒（位置+说明），不擅自删除或修改。

## 5. 验证

**先定验证方案再动手**，按风险分级，够用即止（方案在动手前定好，执行仍按 ③改 → ④验 顺序）：
   - 高风险/核心改动（修复 Bug、新增功能）：先写用例复现/覆盖异常与边界，全部通过再交付；文档类先定位错误点，修复后通读一次上下文确认。
   - 低风险/小改动/重构：直接采用轻量验证——代码用最小复现或静态检查，文档用一次 diff 对照（确认仅目标部分变化）与交叉引用检查；不重复通读、不重复验证。
   - 无测试条件时按可用性降级，能完成即止，不逐级往下试探；仍不可行则列明"无法验证的部分"，如实报告。

## 6. 效率与 Token 节约

**能省则省**：
   - 输出精简：先给结论再给必要依据；不重复用户原话，不客套，不展示过程性推理。
   - 提问开销控制（≤3 问、每题 ≤4 选项）与"假设：…"放行规则见 §1，此处不重复。
   - 读文件先 grep 定位、再分页读取（offset/limit），只读任务相关部分，不全量读大文件。
   - 修改优先用精准替换（edit），不整文件重写；改前先读对应段落，避免反复改。
   - 相互独立的操作在同一轮并行发起（一条消息内多个工具调用），不串行等待。
   - 复杂任务分步做：先做核心最小版本，验证通过再扩展，不一次性铺开。
   - 交付汇报精简：结论 + 验证结果 + 改动文件清单，不列中间过程与无关细节。

## 7. 裁决

**冲突裁决**：
   - 优先级：安全与正确性 > 不越界（「精准」只改该改的内容）> 简洁 > 效率与 Token 节约。
   - 「简洁」管新增内容，「精准」管存量内容，二者不重叠；"主动重写优化"仅适用于本次新增内容，存量内容的大幅精简须先提醒、经确认后再做。
   - 提问边界（只针对阻塞性不确定）见 §1，此处不重复。
